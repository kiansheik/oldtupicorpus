# Session Handoff: GitHub Pages Data Files

## Goal

Fix the deployed GitHub Pages dictionary/corpus viewer after the app loaded but
its `data/*.json(.gz)` fetches returned HTTP 404.

## Files Inspected

- `frontend/src/lib.js`
- `frontend/src/App.jsx`
- `site/index.html`
- `site/data/.gitignore`
- `scripts/deploy_gh_pages.sh`
- `.gh-pages-worktree/`
- `/Users/kian/.codex/attachments/fc89532a-4ea7-4eaa-b1b2-95712bf5cd30/pasted-text.txt`

## Files Changed

- `scripts/deploy_gh_pages.sh`
- `docs/agent/current-state.md`
- `docs/agent/log.md`
- `docs/agent/session-handoffs/2026-08-28T07-57-36-0300-gh-pages-data-files.md`

## Commands Run

- `git check-ignore -v site/data/dictionary_entries.json site/data/dictionary_entries.json.gz site/data/rendered_corpus.json site/data/rendered_corpus.json.gz`
- `git -C .gh-pages-worktree ls-files data`
- `git -C .gh-pages-worktree status --short --ignored data`
- `bash -n scripts/deploy_gh_pages.sh`
- `make dict`
- `make frontend-build`
- `make deploy-gh-pages`
- `curl -I https://kiansheik.io/oldtupicorpus/`
- `curl -I https://kiansheik.io/oldtupicorpus/data/dictionary_entries.json`
- `curl -I https://kiansheik.io/oldtupicorpus/data/dictionary_entries.json.gz`
- `curl -I https://kiansheik.io/oldtupicorpus/data/rendered_corpus.json`
- `curl -I https://kiansheik.io/oldtupicorpus/data/rendered_corpus.json.gz`
- `curl -I https://kiansheik.io/oldtupicorpus/data/navarro_dict.json.gz`

## What Worked

- The browser log showed the app bundle loading from
  `https://kiansheik.io/oldtupicorpus/`, but required data URLs returned HTTP
  404.
- The Pages worktree showed only `data/.gitignore` tracked. The generated JSON
  and gzip data files were present locally but ignored by `data/.gitignore`.
- `scripts/deploy_gh_pages.sh` now verifies that required dictionary/corpus
  data artifacts exist before deploy.
- The deploy script removes `data/.gitignore` from the Pages worktree after
  syncing `SITE_DIR`, so `git add -A` stages generated data files.
- `make deploy-gh-pages` pushed commit `0feadb2` to `gh-pages`, deleting
  `data/.gitignore` and adding the four required data artifacts.
- After propagation, the custom-domain URLs for dictionary and rendered-corpus
  JSON and gzip artifacts returned HTTP 200.

## What Failed

- Initial live checks right after the push still returned 404 for data files
  until GitHub Pages propagation caught up.
- `git ls-remote --heads origin gh-pages` failed inside the sandbox earlier
  with SSH/network restrictions, but the approved `make deploy-gh-pages` command
  pushed successfully.

## Remaining Questions

- Superseded by the later Navarro sidecar deploy handoff: `navarro_dict.json`
  and `navarro_dict.json.gz` are now generated and published.
- The source branch remains dirty with unrelated Araujo/morphology work.

## Suggested Next Prompt

Open the Pages URL in a browser and confirm the dictionary and corpus tabs load
entries from the now-published data files.
