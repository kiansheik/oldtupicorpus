# Ground-truth tail command

## Goal

Add a make command to print the last N saved ground-truth records and their
source commands for a selected `.tu.py` file, defaulting to the last edited.

## Files inspected

`Makefile`, `docs/agent/{index,current-state,repo-map,open-questions,source-authoring}.md`,
`authoring/{records,source_annotations,ground_truth_cli}.py`, both historic
`.tu.py` files, and their generated JSONL records.

## Files changed

`Makefile`, `authoring/last_ground_truth.py`, `tests/last_ground_truth_test.py`,
`docs/agent/{source-authoring,current-state,log}.md`, and this handoff.

## Commands run

`make last-ground-truth`, `make last-ground-truth SOURCE=historic/bettendorff_compendio.tu.py N=2`,
`python3 -m authoring.last_ground_truth --count 0`,
`python3 -m unittest tests.last_ground_truth_test`, `git diff --check`,
`make help`, `python3 -m black --check authoring/last_ground_truth.py tests/last_ground_truth_test.py`.

## What worked

The default selected the current Araujo file and printed records 71–73.
The explicit Bettendorff selection printed records 39–40. The focused tests
passed; invalid N produced a clear error.

## What failed

The Black check could not run because the active Python environment has no
`black` module. The Araujo source currently has four
expressions beyond saved record 73; this command reports the gap and does not
regenerate the artifact.

## Remaining questions

None for this command. The four Araujo expressions still need the normal
human editorial and regeneration workflow.

## Suggested next prompt

Review the next Araujo expression for ground-truth inclusion.
